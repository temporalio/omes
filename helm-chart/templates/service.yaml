apiVersion: v1
kind: Service
metadata:
  name: {{ include "temporal-omes-worker.fullname" . }}
  labels:
    {{- include "temporal-omes-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "temporal-omes-worker.selectorLabels" . | nindent 4 }}
