apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "temporal-omes-worker.fullname" . }}-test-connection"
  labels:
    {{- include "temporal-omes-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "temporal-omes-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
